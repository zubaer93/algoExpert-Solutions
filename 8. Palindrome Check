function isPalindrome(string) {
	let left = 0, right = string.length - 1, palindrome = true;
	while (left < right) {
		if (string[left] == string[right]) {
			left++;
			right--;
		} else {
			palindrome = false;
			break;
		}
	}
	return palindrome;
}
exports.isPalindrome = isPalindrome;
